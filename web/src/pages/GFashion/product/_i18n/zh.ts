import { _spec } from './_spec'

export const zh: typeof _spec = {
  product: {
    total_count: '一共找到{{totalCount}}个产品',
    sort: {
      price_desc: '按价格 (从高到低)',
      price_asc: '按价格 (从低到高)',
      popularity_desc: '按销量 (从高到低)',
      popularity_asc: '按销量 (从低到高)'
    },
    filter: {
      title: '按以下条件筛选:'
    }
  }
}
